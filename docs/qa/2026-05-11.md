# qa report — 2026-05-11

## verified

- branch: `overnight/2026-05-11-hardening`
- local command: `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ./gradlew --no-daemon :app:assembleDebug :app:testDebugUnitTest --stacktrace`
- result: green
- pact check: no manifest permissions, no network-adjacent permissions, no firebase/gms/analytics dependency

## work reviewed

- root pact link added to the README
- build workflow added
- intent journal codec now has JVM tests for append ordering, 500-entry cap, clear, and malformed JSON
- launcher preferences now have a pure DataStore-shaped mapper tested for slot-label/friction round-trip
- string resource uppercase guard added for future user-facing strings

## risks

- most UI copy is hardcoded in Compose, so the string-resource uppercase test is a narrow guard
- no emulator was reachable during the run, so no new home-screen interaction pass or screenshots were captured

## next improvements

- extract common verb copy into resources or a small typed copy table so lowercase-copy tests cover more UI
- add JVM coverage for default-slot seeding and package scanner filtering
